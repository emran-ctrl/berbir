# Rust / Cargo build output
/target
**/*.rs.bk

# Compiled frontend bundle (rebuilt with `trunk build`)
/crates/app/dist

# Local environment overrides (see `.env.example`)
.env

# SQLite databases
*.db
*.db-wal
*.db-shm

# Editor / OS
.vscode/
.idea/
*.swp
.DS_Store
